pipeline {

     agent any

        stages {
            stage('Build') {
                steps {
                    #!/bin/bash -ilex
                    sh 'make'
                }
            }
        }
}